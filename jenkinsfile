pipeline {
    agent any
    stages {
        stage("Build"){
            steps{
                sh "docker-compose up --build"
            }
        }
        stage('Deploy'){
            steps{
                sh "echo \$(grep 'version:' pubspec.yaml | awk '{print \$2}') > versions.txt"
            }
        }
    }
}  