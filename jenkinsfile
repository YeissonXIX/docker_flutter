pipeline {
    agent any
    stages {
        stage('Build'){
            steps{
                sh 'docker --version'
                sh 'docker build -t wms_mobile_image .'
                sh 'docker images'
                sh 'docker run --rm --mount source=$PWD,target=/app wms_mobile_image'
            }
        }
    }
}  