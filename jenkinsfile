pipeline {
    agent any
    stages {
        stage("Build"){
            steps{
                //sh "docker-compose up --build"
                sh 'echo ${env.RECEIVINGBASEAPI}'
            }
        }
       /*  stage('Deploy'){
            steps{
                sh "echo \$(grep 'version:' pubspec.yaml | awk '{print \$2}' | awk -F '+' '{print \$1}') \$(date) > versions.txt"
            }
        } */
    }
}  