pipeline {
    agent any
    stages {
        stage('Build'){
            steps{
                sh 'docker --version'
                sh 'docker build -t wmb_mobile_image .'
                sh 'docker images'
                sh 'docker run --rm -v ./app:./ wmb_mobile_image'
            }
        }
    }
}  